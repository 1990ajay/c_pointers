#include <stdio.h>

void main ()
{
int a = 10;
printf(" The value of A after before calling the function %d ", a);
X(a);
printf(" The value of A after increment %d ", a);
	}


void X(int x) {
	x++;
  	int a =5;		
	printf(" The value of X inside the loop is  %d ", x);
	
	printf(" The value of A inside the loop %d ", a);
	
}
